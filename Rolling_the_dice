import random
while True:
   choice=input("roll the dice : ")
   if choice.lower() == 'y' :
        die1=random.randint(1,6)
        die2=random.randint(1,6)
        print(f'({die1} , {die2})')
   elif chioce.lower()=='n'
    print("thankyou")
    break
   else:
    print('invalid chiice')
 
 
    
